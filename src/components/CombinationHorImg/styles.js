import { StyleSheet } from 'react-native';

export default styles = StyleSheet.create({
  combinationImg:{
    flexDirection: 'row',
    alignItems: 'center'
  },
  trigger:{
    flex: 1
  },
  action:{
    flex: 1
  }
});
